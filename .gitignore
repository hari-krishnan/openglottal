# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
*.env

# Data & weights
*.pt
*.pth
*.onnx
*.avi
*.mp4
data/

# All trained models and YOLO run outputs (single dir for U-Net + YOLO)
outputs/

# TensorBoard / legacy YOLO runs
runs/

# Notebooks
.ipynb_checkpoints/


# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Local README preview (generated for browser preview)
README_preview.html

# LaTeX build artifacts (regenerate with latexmk)
*.aux
*.log
*.out
*.fls
*.fdb_latexmk
*.blg
*.bbl
*.spl
*.synctex.gz
*.synctex(busy)
*.toc
*.lof
*.lot
*.nav
*.snm
*.vrb
*.bcf
*.run.xml
*.idx
*.ind
*.ilg
*.glo
*.gls
*.glg
*.acn
*.acr
*.alg
*-blx.bib
# LaTeX build output (if building under paper_cross/)
paper_cross/%OUTDIR%/

# Generated evaluation/output (reproducible via scripts)
results/
eval_results*.json

# Local dataset copies (large; not for git)
GIRAFE/
BAGLS/
# YOLO training exports — keep dataset.yaml portable; images/labels are huge
yolo_data*/images/
yolo_data*/labels/

# Hugging Face / caches
.hf_home/

# Tooling & scratch
.claude/
.pytest_cache/
coverage.xml
.coverage
htmlcov/
uncommitted_changes.diff
*.patch

# LaTeX / paper build outputs (keep sources in paper_cross/)
paper_cross/*.zip
paper_cross/main.pdf
paper_cross/arxiv_submit/

# Optional local metadata at repo root
/dataset-metadata.json
